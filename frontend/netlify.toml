[build]
  command = "npm ci && ng build --configuration production"
  publish = "dist/accident-book"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

