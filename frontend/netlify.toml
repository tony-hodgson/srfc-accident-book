[build]
  command = "npm install && ng build --configuration production"
  publish = "dist/accident-book"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

